# Copy to .env and fill in real values. NEVER commit the real .env.
# Supabase (data + image storage + expert review library)
SUPABASE_URL=
SUPABASE_SERVICE_KEY=

# OpenAI (Sage chat + care guide)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Reviewer dashboard access code
REVIEWER_PIN=314159

# Point at your model service to run LIGHT (proxy) mode — no torch needed.
# Leave empty to load the CNN/CLIP models locally (full features, needs torch).
MODEL_API_URL=https://vKhaled02-agrocure-api.hf.space
